# One-command MySQL (+ Adminer DB browser) for local development.
#   docker compose up -d      # start everything in the background
#   docker compose down       # stop it (data is kept in the named volume)
#   docker compose down -v     # stop it and DELETE all data
#
# MySQL matches the DATABASE_URL in .env.example:
#   mysql://root:spynder@localhost:3306/spynder
# Adminer (browse the DB) runs at http://localhost:8080
#   System: MySQL · Server: mysql · Username: root · Password: spynder · Database: spynder

services:
  mysql:
    image: mysql:8.4
    container_name: spynder-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: spynder
      MYSQL_DATABASE: spynder
    ports:
      - "3306:3306"
    volumes:
      - spynder-mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pspynder"]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer:5
    container_name: spynder-adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: mysql
    depends_on:
      mysql:
        condition: service_healthy

volumes:
  spynder-mysql-data:
